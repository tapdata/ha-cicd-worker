name: TapData Rollback

on:
  workflow_call:
    inputs:
      target_env:
        description: 'Target environment'
        required: true
        type: string
      last_stable_tag:
        description: 'Tag to rollback to'
        required: true
        type: string

jobs:
  rollback:
    name: Execute Rollback
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - name: Placeholder
        run: |
          echo "=== Rollback Placeholder ==="
          echo "Target Environment: ${{ inputs.target_env }}"
          echo "Rollback Tag:       ${{ inputs.last_stable_tag }}"
          echo "TODO: Implement rollback logic"
          echo "=== Done ==="
