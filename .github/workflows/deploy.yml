name: Tapdata配置部署

on:
  workflow_dispatch:
    inputs:
      env:
        description: '环境'
        required: true
        type: choice
        options:
          - dev
          - lpt
          - preprod
          - prod
      project_group:
        description: '项目分组'
        required: true
        type: choice
        options:
          - patient
          - hospital

env:
  SHARED_DIR: /tmp/tapdata-deploy-${{ github.run_id }}

jobs:
  prepare:
    name: 准备配置
    runs-on: self-hosted
    timeout-minutes: 5
    outputs:
      config_repo: ${{ steps.get_repo.outputs.repo }}
      base_url: ${{ steps.get_url.outputs.base_url }}
      tar_path: ${{ steps.create_tar.outputs.tar_path }}
    steps:
      - name: 检出当前仓库代码
        uses: actions/checkout@v4

      - name: 创建共享目录
        run: |
          mkdir -p ${{ env.SHARED_DIR }}
          echo "共享目录: ${{ env.SHARED_DIR }}"

      - name: 获取配置仓库名称
        id: get_repo
        run: |
          REPO=$(grep "^${{ inputs.project_group }}=" conf/project.conf | cut -d'=' -f2)
          if [ -z "$REPO" ]; then
            echo "错误：未找到项目分组 ${{ inputs.project_group }} 的配置仓库"
            exit 1
          fi
          echo "配置仓库: $REPO"
          echo "repo=$REPO" >> $GITHUB_OUTPUT

      - name: 检出配置仓库代码
        uses: actions/checkout@v4
        with:
          repository: ${{ steps.get_repo.outputs.repo }}
          ref: main
          path: config-repo
          token: ${{ secrets.PAT_TOKEN }}

      - name: 压缩配置仓库为tar
        id: create_tar
        run: |
          cd config-repo
          TAR_PATH="${{ env.SHARED_DIR }}/config.tar.gz"
          tar -czf "$TAR_PATH" .
          cd ..
          ls -lh "$TAR_PATH"
          echo "tar_path=$TAR_PATH" >> $GITHUB_OUTPUT

      - name: 获取Tapdata地址
        id: get_url
        run: |
          BASE_URL=$(grep "^${{ inputs.env }}=" conf/env.conf | cut -d'=' -f2)
          if [ -z "$BASE_URL" ]; then
            echo "错误：未找到环境 ${{ inputs.env }} 的Tapdata地址"
            exit 1
          fi
          echo "Tapdata地址: $BASE_URL"
          echo "base_url=$BASE_URL" >> $GITHUB_OUTPUT

  import:
    name: 导入配置
    runs-on: self-hosted
    timeout-minutes: 20
    needs: prepare
    outputs:
      record_id: ${{ steps.import.outputs.record_id }}
    steps:
      - name: 检出当前仓库代码
        uses: actions/checkout@v4

      - name: 导入配置到Tapdata
        id: import
        run: |
          chmod +x scripts/tapdata-import.sh
          scripts/tapdata-import.sh \
            "${{ needs.prepare.outputs.base_url }}" \
            "${{ needs.prepare.outputs.tar_path }}"

  verify:
    name: 验证结果
    runs-on: self-hosted
    timeout-minutes: 5
    needs: [prepare, import]
    steps:
      - name: 检出当前仓库代码
        uses: actions/checkout@v4

      - name: 检查导入状态
        id: verify_status
        run: |
          chmod +x scripts/tapdata-check.sh
          scripts/tapdata-check.sh \
            "${{ needs.prepare.outputs.base_url }}" \
            "${{ needs.import.outputs.record_id }}"

      - name: 清理共享目录
        if: always()
        run: |
          if [ -d "${{ env.SHARED_DIR }}" ]; then
            rm -rf "${{ env.SHARED_DIR }}"
            echo "已清理共享目录: ${{ env.SHARED_DIR }}"
          fi

  report:
    name: 生成报告
    runs-on: self-hosted
    timeout-minutes: 2
    if: always()
    needs: [prepare, import, verify]
    steps:
      - name: 生成部署报告
        run: |
          # 判断整体部署状态（用于报告内容，不影响此 job 的成功/失败）
          if [ "${{ needs.prepare.result }}" == "success" ] && \
             [ "${{ needs.import.result }}" == "success" ] && \
             [ "${{ needs.verify.result }}" == "success" ]; then
            OVERALL_STATUS="success"
            STATUS_ICON="✅"
            STATUS_TEXT="成功"
          else
            OVERALL_STATUS="failure"
            STATUS_ICON="❌"
            STATUS_TEXT="失败"
          fi

          # 输出到 GitHub Actions Summary
          cat >> $GITHUB_STEP_SUMMARY << EOF
          # 📊 Tapdata 配置部署报告

          ## ${STATUS_ICON} 整体状态: ${STATUS_TEXT}

          ### 🔧 部署信息
          | 项目 | 值 |
          |------|-----|
          | 环境 | \`${{ inputs.env }}\` |
          | 项目分组 | \`${{ inputs.project_group }}\` |
          | 配置仓库 | \`${{ needs.prepare.outputs.config_repo }}\` |
          | Tapdata 地址 | ${{ needs.prepare.outputs.base_url }} |
          | 触发者 | @${{ github.actor }} |
          | 运行编号 | #${{ github.run_number }} |

          ### 📋 执行结果
          | Job | 状态 |
          |-----|------|
          | 准备配置 | ${{ needs.prepare.result == 'success' && '✅ 成功' || '❌ 失败' }} |
          | 导入配置 | ${{ needs.import.result == 'success' && '✅ 成功' || '❌ 失败' }} |
          | 验证结果 | ${{ needs.verify.result == 'success' && '✅ 成功' || '❌ 失败' }} |

          EOF

          # 如果部署失败，添加失败原因分析
          if [ "$OVERALL_STATUS" == "failure" ]; then
            cat >> $GITHUB_STEP_SUMMARY << EOF
          ### 💡 失败原因分析
          EOF
            if [ "${{ needs.prepare.result }}" != "success" ]; then
              echo "- ⚠️ **准备配置阶段失败**：请检查配置仓库是否存在或配置文件是否正确" >> $GITHUB_STEP_SUMMARY
            fi
            if [ "${{ needs.import.result }}" != "success" ]; then
              echo "- ⚠️ **导入配置阶段失败**：请检查 Tapdata API 是否可访问或 access_token 是否有效" >> $GITHUB_STEP_SUMMARY
            fi
            if [ "${{ needs.verify.result }}" != "success" ]; then
              echo "- ⚠️ **验证结果阶段失败**：请查看日志了解导入失败的详细原因" >> $GITHUB_STEP_SUMMARY
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 🎉 配置已成功部署到 \`${{ inputs.env }}\` 环境！" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          # 添加相关链接
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ### 🔗 相关链接
          - [Workflow 运行详情](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          EOF

          if [ "${{ needs.import.outputs.record_id }}" != "" ]; then
            echo "- 导入记录 ID: \`${{ needs.import.outputs.record_id }}\`" >> $GITHUB_STEP_SUMMARY
          fi

          # 添加时间信息
          END_TIME=$(date '+%Y-%m-%d %H:%M:%S')
          cat >> $GITHUB_STEP_SUMMARY << EOF

          ### ⏰ 时间信息
          - 结束时间: ${END_TIME}

          ---
          *报告生成时间: ${END_TIME}*
          EOF

          # 同时输出到控制台日志
          echo "=========================================="
          echo "📊 Tapdata 配置部署报告"
          echo "=========================================="
          echo ""
          echo "部署整体状态: ${STATUS_ICON} ${STATUS_TEXT}"
          echo "环境: ${{ inputs.env }}"
          echo "项目分组: ${{ inputs.project_group }}"
          echo ""
          echo "✅ 报告生成成功！详细报告已输出到 GitHub Actions Summary"
          echo "=========================================="

          # 注意：不再根据部署状态退出，只要报告生成成功，此 job 就成功
          # 这样即使前面的步骤失败，报告 job 也会显示为成功（绿色）
