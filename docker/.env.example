# GitHub Actions Runner 配置示例
# 复制此文件为 .env 并填写实际值

# 必需参数
# Runner Token - 从 GitHub 仓库设置中获取
# 路径: Settings -> Actions -> Runners -> New self-hosted runner
RUNNER_TOKEN=your_runner_token_here

# 仓库 URL
RUNNER_REPO_URL=https://github.com/your-org/your-repo

# 可选参数
# Runner 名称（默认: docker-runner）
RUNNER_NAME=docker-runner

# Runner 标签（默认: self-hosted,Linux,X64,docker）
RUNNER_LABELS=self-hosted,Linux,X64,docker

# 工作目录（默认: _work）
RUNNER_WORK_DIR=_work

